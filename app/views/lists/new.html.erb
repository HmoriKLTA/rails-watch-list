<div class="container">
  <h1>Create a new list</h1>
  <%= simple_form_for [@list] do |f| %>
    <%= f.input :name %>
    <%= f.submit 'Create', class: 'btn btn-danger mt-3' %>
  <% end %>

  <%= link_to "Back to Home", lists_path, class: "btn btn-gradient" %>
</div>
